import {
  site_name,
  author,
  description,
  locale,
  locale_region,
  title,
} from "./Content";
export const AppConfig = {
  site_name,
  title,
  description,
  author,
  locale_region,
  locale,
};
